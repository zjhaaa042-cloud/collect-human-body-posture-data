# 一键安装依赖

换电脑或第一次运行项目时，先双击运行：

```text
install_deps.bat
```

它会自动完成：

- 创建或复用项目根目录下的 `.venv` Python 虚拟环境
- 安装 `requirements.txt` 里的后端依赖
- 进入 `frontend` 目录安装前端依赖

安装完成后，再双击运行：

```text
go.bat
```

如果提示找不到 Python 或 npm，请先安装：

- Python 3.10+
- Node.js 18+ LTS

如果 `PyAudio` 或 `pyorbbecsdk2` 安装失败，通常需要先安装对应的系统组件，例如 Microsoft C++ Build Tools、相机 SDK 或驱动，然后重新运行 `install_deps.bat`。
